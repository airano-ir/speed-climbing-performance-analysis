# راهنمای کاربری رابط بررسی دستی مسابقات
## Manual Review Interface - User Guide (Persian/Farsi)

**تاریخ**: 1404/08/26 (2025-11-16)
**نسخه**: 1.0
**زبان**: فارسی

---

## 📋 فهرست مطالب

1. [معرفی](#معرفی)
2. [پیش‌نیازها](#پیش‌نیازها)
3. [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
4. [راهنمای استفاده](#راهنمای-استفاده)
5. [اصلاح خطاهای تشخیص مسابقه](#اصلاح-خطاهای-تشخیص-مسابقه)
6. [مثال‌های کاربردی](#مثال‌های-کاربردی)
7. [سوالات متداول](#سوالات-متداول)
8. [عیب‌یابی](#عیب‌یابی)

---

## معرفی

### چرا این رابط ساخته شد؟

در حین آنالیز خودکار ویدئوهای سنگنوردی سرعتی، **74 مسابقه از 188 مسابقه (39.4%)** با خطاهای تشخیص مواجه شدند. این رابط کاربری برای اصلاح دستی این خطاها طراحی شده است.

### ویژگی‌های کلیدی

✅ **رابط دوزبانه**: پشتیبانی کامل از فارسی و انگلیسی
✅ **پخش‌کننده ویدئو**: ناوبری فریم به فریم با دقت بالا
✅ **اعتبارسنجی خودکار**: بررسی صحت داده‌های اصلاح شده
✅ **ردیابی پیشرفت**: نمایش آمار کامل پیشرفت بررسی
✅ **پشتیبان‌گیری خودکار**: ذخیره نسخه پشتیبان قبل از هر تغییر
✅ **گسترش‌پذیری**: امکان افزودن مسابقات جدید در کمتر از 5 دقیقه

---

## پیش‌نیازها

### نرم‌افزارهای مورد نیاز

```bash
# Python نسخه 3.8 یا بالاتر
python --version

# کتابخانه‌های مورد نیاز:
streamlit >= 1.28.0
opencv-python >= 4.8.0
pyyaml >= 6.0
numpy >= 1.24.0
```

### ساختار پوشه‌ها

```
Speed-Climbing-Performance-Analysis/
├── configs/
│   └── manual_review_config.yaml       # فایل پیکربندی
├── data/
│   ├── race_segments/                  # ویدئوهای مسابقات
│   │   ├── chamonix_2024/
│   │   ├── villars_2024/
│   │   ├── innsbruck_2024/
│   │   └── zilina_2025/
│   └── manual_review/
│       ├── progress_tracker.csv        # ردیاب پیشرفت
│       └── templates/                  # قالب‌های کمکی
└── scripts/
    └── review_interface/               # کدهای رابط کاربری
        ├── app.py                      # اپلیکیشن اصلی
        ├── config.py                   # مدیر پیکربندی
        ├── progress.py                 # ردیاب پیشرفت
        ├── metadata_manager.py         # مدیر متادیتا
        ├── video_player.py             # پخش‌کننده ویدئو
        └── validators.py               # اعتبارسنجی
```

---

## نصب و راه‌اندازی

### مرحله 1: نصب کتابخانه‌ها

```bash
# در پوشه اصلی پروژه:
cd /path/to/Speed-Climbing-Performance-Analysis

# نصب کتابخانه‌های مورد نیاز:
pip install streamlit opencv-python pyyaml numpy
```

### مرحله 2: بررسی فایل‌های پیکربندی

```bash
# بررسی وجود فایل‌های ضروری:
ls configs/manual_review_config.yaml
ls data/manual_review/progress_tracker.csv
```

### مرحله 3: اجرای رابط کاربری

```bash
# رفتن به پوشه رابط کاربری:
cd scripts/review_interface

# اجرای اپلیکیشن Streamlit:
streamlit run app.py
```

اپلیکیشن در مرورگر شما باز می‌شود (معمولاً در آدرس: `http://localhost:8501`)

---

## راهنمای استفاده

### 1. انتخاب زبان

در نوار کناری (sidebar)، از منوی **"Language / زبان"** زبان مورد نظر خود را انتخاب کنید:
- 🇬🇧 English
- 🇮🇷 فارسی

### 2. مشاهده آمار پیشرفت

در بخش **"📊 آمار پیشرفت"** می‌توانید موارد زیر را ببینید:
- **مجموع مسابقات**: تعداد کل مسابقات نیازمند بررسی
- **تکمیل شده**: تعداد مسابقات اصلاح شده
- **در انتظار**: تعداد مسابقات باقی‌مانده
- **بحرانی**: تعداد مسابقات با اولویت بالا

### 3. فیلتر کردن مسابقات

از فیلترهای زیر برای محدود کردن لیست مسابقات استفاده کنید:

- **مسابقه**: انتخاب یک مسابقه خاص (مثلاً Chamonix 2024)
- **اولویت**:
  - Critical (1): خطاهای بحرانی (منفی یا نزدیک به صفر)
  - High (2): مسابقات Zilina 2025 (شکست سیستماتیک)
  - Medium (3): زمان خیلی کوتاه یا بلند
  - Low (4): سایر مشکلات
- **وضعیت**:
  - فقط در انتظار
  - همه
  - تکمیل شده
  - رد شده

### 4. انتخاب مسابقه برای بررسی

از منوی کشویی **"انتخاب مسابقه برای بررسی"** یک مسابقه را انتخاب کنید. هر مسابقه با فرمت زیر نمایش داده می‌شود:

```
[P1] Speed_finals_Villars_2024_race015 (-2.57s → Negative duration - critical error)
```

- `[P1]`: اولویت (1 = بحرانی)
- `-2.57s`: مدت زمان تشخیص داده شده (منفی = خطا!)
- توضیح مشکل

---

## اصلاح خطاهای تشخیص مسابقه

### مرحله 1: بررسی اطلاعات مسابقه

بعد از انتخاب مسابقه، سه بخش اطلاعاتی نمایش داده می‌شود:

#### 📁 اطلاعات مسابقه
- شناسه مسابقه
- نام مسابقه
- اولویت
- وضعیت فعلی

#### ⚠️ مشکل تشخیص
- توضیح مشکل
- مدت زمان تشخیص داده شده
- تعداد فریم‌ها
- سطح اطمینان شروع و پایان

#### ✅ وضعیت اصلاح
- آیا قبلاً اصلاح شده؟
- مقادیر اصلاح شده (در صورت وجود)

### مرحله 2: استفاده از پخش‌کننده ویدئو

**ویژگی‌های پخش‌کننده:**

1. **اسلایدر فریم**: برای حرکت سریع در ویدئو
2. **دکمه‌های ناوبری**:
   - ⏮️ -30: 30 فریم به عقب
   - ⬅️ -5: 5 فریم به عقب
   - ◀️ -1: 1 فریم به عقب
   - ▶️ +1: 1 فریم به جلو
   - ➡️ +5: 5 فریم به جلو
   - ⏭️ +30: 30 فریم به جلو

3. **نمایش اطلاعات**:
   - زمان فعلی (ثانیه)
   - شماره فریم فعلی
   - تصویر فریم فعلی

### مرحله 3: علامت‌گذاری فریم‌های شروع و پایان

#### 🟢 فریم شروع:
1. با استفاده از پخش‌کننده، **لحظه دقیق شروع مسابقه** را پیدا کنید
   - معمولاً: لحظه‌ای که ورزشکار شروع به حرکت می‌کند
   - یا: لحظه شنیدن صدای بوق شروع
2. روی دکمه **"📍 علامت‌گذاری فریم فعلی به عنوان شروع"** کلیک کنید
3. شماره فریم خودکار در کادر **"فریم شروع جدید"** قرار می‌گیرد

#### 🔴 فریم پایان:
1. **لحظه دقیق پایان مسابقه** را پیدا کنید
   - معمولاً: لحظه‌ای که دست ورزشکار به دکمه بالای دیوار می‌رسد
   - یا: لحظه روشن شدن چراغ پایان
2. روی دکمه **"📍 علامت‌گذاری فریم فعلی به عنوان پایان"** کلیک کنید
3. شماره فریم خودکار در کادر **"فریم پایان جدید"** قرار می‌گیرد

> **نکته مهم**: می‌توانید شماره فریم‌ها را دستی هم تغییر دهید.

### مرحله 4: بررسی اعتبارسنجی

پس از انتخاب فریم‌های شروع و پایان، سیستم خودکار موارد زیر را بررسی می‌کند:

✅ **معتبر**: مدت زمان در بازه مجاز (4.5s - 15s)
⚠️ **هشدار**: مدت زمان کمتر از حداقل یا بیشتر از حداکثر
❌ **خطا**: مدت زمان منفی یا ترتیب فریم‌ها اشتباه

**بازه‌های مرجع:**
- حداقل: 4.5 ثانیه (کمی کمتر از رکورد جهانی)
- حداکثر: 15 ثانیه (شامل ورزشکاران آهسته‌تر + سقوط)
- رکورد جهانی مردان: 5.00s (Reza Alipour, 2023)
- رکورد جهانی زنان: 6.53s (Aleksandra Miroslaw, 2024)

### مرحله 5: وارد کردن دلیل اصلاح

در بخش **"📝 جزئیات اصلاح"**:

1. **دلیل اصلاح** (اجباری):
   - مثال: "سقوط ورزشکار به عنوان پایان تشخیص داده شد"
   - مثال: "گرم کردن قبل از مسابقه شامل شد"
   - مثال: "استارت اشتباه / false start"

2. **یادداشت‌های بازبین** (اختیاری):
   - توضیحات تکمیلی
   - جزئیات بیشتر درباره مشکل
   - نکاتی برای بررسی‌های آینده

### مرحله 6: ذخیره اصلاح

سه گزینه دارید:

1. **💾 ذخیره اصلاح**:
   - اطلاعات جدید در فایل metadata ذخیره می‌شود
   - نسخه پشتیبان از فایل قبلی ایجاد می‌شود (`.bak`)
   - وضعیت در progress tracker به "تکمیل شده" تغییر می‌کند
   - آمار پیشرفت به‌روز می‌شود

2. **⏭️ رد کردن مسابقه**:
   - اگر نمی‌توانید مسابقه را اصلاح کنید
   - وضعیت به "رد شده" تغییر می‌کند
   - می‌توانید بعداً به آن برگردید

3. **🔄 بازنشانی**:
   - پاک کردن فریم‌های علامت‌گذاری شده
   - بازگشت به مقادیر اولیه

---

## مثال‌های کاربردی

### مثال 1: اصلاح مسابقه با سقوط ورزشکار

**مشکل**: سیستم خودکار، لحظه سقوط ورزشکار را به عنوان پایان مسابقه تشخیص داده است.

**گام‌های اصلاح**:
1. انتخاب مسابقه: `Speed_finals_Chamonix_2024_race001`
2. مشاهده ویدئو: مدت زمان تشخیص داده شده 1.77s (خیلی کوتاه!)
3. پیدا کردن فریم شروع واقعی:
   - استفاده از پخش‌کننده برای یافتن لحظه شروع
   - علامت‌گذاری فریم (مثلاً فریم 14049)
4. پیدا کردن فریم پایان واقعی:
   - پیدا کردن لحظه‌ای که دست ورزشکار به دکمه بالا می‌رسد
   - علامت‌گذاری فریم (مثلاً فریم 14244)
5. بررسی مدت زمان جدید: 6.5s ✅ (معتبر)
6. وارد کردن دلیل: "سقوط ورزشکار به عنوان پایان تشخیص داده شد"
7. ذخیره اصلاح

### مثال 2: اصلاح مسابقه با گرم‌کردن قبل از شروع

**مشکل**: سیستم خودکار، حرکات گرم‌کردن را به عنوان شروع مسابقه تشخیص داده است.

**گام‌های اصلاح**:
1. انتخاب مسابقه: `Speed_finals_Innsbruck_2024_race010`
2. مشاهده ویدئو: مدت زمان 12.00s (خیلی بلند)
3. یافتن شروع واقعی:
   - رد کردن حرکات گرم‌کردن
   - پیدا کردن لحظه شنیدن بوق شروع
4. یافتن پایان واقعی:
   - لحظه رسیدن به دکمه بالا
5. بررسی مدت زمان جدید: 7.5s ✅
6. دلیل: "گرم کردن قبل از مسابقه شامل شد"
7. ذخیره

### مثال 3: اصلاح مسابقه با مدت زمان منفی

**مشکل**: خطای بحرانی - مدت زمان منفی!

**گام‌های اصلاح**:
1. انتخاب مسابقه: `Speed_finals_Villars_2024_race015`
2. مشاهده ویدئو: مدت زمان -2.57s (غیرممکن!)
3. بررسی کامل ویدئو:
   - پیدا کردن شروع واقعی
   - پیدا کردن پایان واقعی
4. اصلاح فریم‌ها
5. بررسی مدت زمان جدید (باید مثبت و در بازه معتبر باشد)
6. دلیل: "خطای تشخیص - ترتیب فریم‌ها اشتباه بود"
7. ذخیره

---

## سوالات متداول

### س: چگونه می‌توانم زبان رابط را تغییر دهم؟
**ج**: از منوی "Language / زبان" در نوار کناری (sidebar) زبان مورد نظر را انتخاب کنید. تمام متن‌ها به‌روز می‌شوند.

### س: آیا می‌توانم یک اصلاح را تغییر دهم؟
**ج**: بله! مسابقات اصلاح شده را می‌توانید دوباره باز کرده و ویرایش کنید. سیستم اطلاعات قبلی را در `correction_metadata` نگه می‌دارد.

### س: فایل‌های پشتیبان کجا ذخیره می‌شوند؟
**ج**: در کنار فایل اصلی metadata با پسوند `.bak`:
```
data/race_segments/chamonix_2024/Speed_finals_Chamonix_2024_race001_metadata.json.bak
```

### س: چگونه می‌توانم مسابقه جدیدی اضافه کنم؟
**ج**: فایل `configs/manual_review_config.yaml` را ویرایش کنید و یک مسابقه جدید به بخش `competitions` اضافه کنید. قالب موجود را دنبال کنید.

### س: آیا می‌توانم چند مسابقه را به صورت دسته‌ای بررسی کنم؟
**ج**: بله! از فیلتر اولویت استفاده کنید تا مسابقات مشابه را ببینید، سپس به ترتیب آن‌ها را بررسی کنید.

### س: مدت زمان بررسی هر مسابقه چقدر است؟
**ج**:
- مسابقات ساده: 2-5 دقیقه
- مسابقات پیچیده (با سقوط، خطا): 5-15 دقیقه
- متوسط: ~10 دقیقه

### س: چگونه می‌دانم همه مسابقات بررسی شده‌اند؟
**ج**: در بخش آمار پیشرفت، وقتی **تکمیل شده = مجموع مسابقات** شود، همه بررسی شده‌اند.

---

## عیب‌یابی

### مشکل: رابط باز نمی‌شود

**راه‌حل**:
```bash
# بررسی نصب Streamlit:
streamlit --version

# اگر نصب نیست:
pip install streamlit

# اجرای مجدد:
cd scripts/review_interface
streamlit run app.py
```

### مشکل: ویدئو نمایش داده نمی‌شود

**راه‌حل**:
1. بررسی وجود فایل ویدئو:
   ```bash
   ls data/race_segments/[competition]/[race_id].mp4
   ```

2. بررسی نصب OpenCV:
   ```bash
   python -c "import cv2; print(cv2.__version__)"
   ```

3. اگر نصب نیست:
   ```bash
   pip install opencv-python
   ```

### مشکل: خطای "FileNotFoundError: Config not found"

**راه‌حل**:
```bash
# بررسی وجود فایل config:
ls configs/manual_review_config.yaml

# اگر وجود ندارد، از قالب استفاده کنید:
cp data/manual_review/templates/new_competition_template.yaml configs/manual_review_config.yaml
```

### مشکل: تغییرات ذخیره نمی‌شوند

**راه‌حل**:
1. بررسی مجوزهای فایل:
   ```bash
   ls -la data/race_segments/[competition]/
   ```

2. اطمینان از دسترسی نوشتن:
   ```bash
   chmod +w data/race_segments/[competition]/[race_id]_metadata.json
   ```

### مشکل: اعتبارسنجی همیشه خطا می‌دهد

**راه‌حل**:
1. بررسی مقادیر در فایل config:
   ```yaml
   validation:
     duration:
       min: 4.5
       max: 15.0
   ```

2. اطمینان از اینکه:
   - فریم پایان > فریم شروع
   - مدت زمان بین 4.5 تا 15 ثانیه
   - فریم‌ها در محدوده ویدئو هستند

---

## پشتیبانی و کمک

### منابع اضافی

- **راهنمای انگلیسی**: `docs/PROMPT_FOR_UI_BUILD_REVIEW_INTERFACE.md`
- **مستندات پروژه**: `MASTER_CONTEXT.md`
- **قالب‌های کمکی**: `data/manual_review/templates/`
- **اسکریپت‌های تست**: `scripts/review_interface/test_app.py`

### گزارش مشکلات

اگر مشکلی پیدا کردید:
1. بررسی بخش عیب‌یابی در بالا
2. بررسی لاگ‌های Streamlit در ترمینال
3. مشاهده فایل‌های پشتیبان (`.bak`)
4. بررسی فایل `progress_tracker.csv` برای ناسازگاری

---

## نتیجه‌گیری

این رابط کاربری یک ابزار حرفه‌ای برای اصلاح دستی خطاهای تشخیص مسابقه است که:

✅ صرفه‌جویی در زمان (10+ ساعت نسبت به اصلاح دستی)
✅ کیفیت داده (اعتبارسنجی خودکار)
✅ قابل استفاده مجدد (برای مسابقات آینده)
✅ قابل گسترش (افزودن ویژگی‌های جدید)

**موفق باشید!** 🎉

---

**پایان راهنما**
**تاریخ آخرین به‌روزرسانی**: 1404/08/26
